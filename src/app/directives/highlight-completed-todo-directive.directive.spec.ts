import { HighlightCompletedTodoDirectiveDirective } from './highlight-completed-todo-directive.directive';

describe('HighlightCompletedTodoDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCompletedTodoDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
